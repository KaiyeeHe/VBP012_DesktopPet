# 金钱豹桌面宠物 - Stickies 版本

## 最新版本特性

### 🎨 全新 Stickies 风格设计

你重写了整个界面,采用了类似 macOS Stickies 的设计理念:

#### 核心改进:
1. **原生窗口拖拽** - 使用 `-webkit-app-region: drag`
   - 整个窗口可以像 Stickies 一样拖拽
   - 不需要自定义拖拽逻辑
   - 更加原生和流畅

2. **固定小窗口** (240x280px)
   - 紧凑的固定尺寸窗口
   - 不再是全屏透明覆盖
   - 更像真正的桌面便签

3. **自动散步功能** 🚶
   - 金钱豹会自动在桌面上小范围走动
   - 每4秒有50%概率移动
   - 向左走时会转身(scaleX(-1))
   - 通过IPC消息控制窗口移动

4. **极简黑白UI**
   - 纯黑白配色方案
   - 圆角白底状态栏
   - 黑色边框和阴影
   - Helvetica Neue 字体

## 技术架构更新

### main.js 改进:
```javascript
// 固定小窗口 (240x280)
const mainWindow = new BrowserWindow({
  width: 240,
  height: 280,
  x: Math.floor(width / 2 - 120),
  y: Math.floor(height / 3 - 140),
  transparent: true,
  frame: false,
  alwaysOnTop: true
})

// 新增IPC消息处理:
ipcMain.on('window-move', (event, { x, y }) => {
  // 控制窗口相对移动
  mainWindow.setPosition(currentX + x, currentY + y)
})

ipcMain.on('window-reset', () => {
  // 重置到左下角
  mainWindow.setPosition(100, height - 300)
})
```

### index.html 新特性:

#### 1. 原生拖拽区域
```css
#main-wrapper {
  -webkit-app-region: drag;  /* 整个区域可拖拽 */
  cursor: grab;
}

#status-bar {
  -webkit-app-region: no-drag;  /* 按钮区域不拖拽 */
}
```

#### 2. 自动散步逻辑
```javascript
function startWandering() {
  setInterval(() => {
    if (state.isWorking && !state.isPaused) {
      if (Math.random() > 0.5) performWalk();
    }
  }, 4000);
}

function performWalk() {
  const moveX = Math.floor((Math.random() - 0.5) * 60);
  const moveY = Math.floor((Math.random() - 0.5) * 20);

  // 转身
  if (moveX < 0) leopard.classList.add('facing-left');
  else leopard.classList.remove('facing-left');

  // 通知主进程移动窗口
  ipcRenderer.send('window-move', { x: moveX, y: moveY });
}
```

#### 3. 极简状态栏设计
- ⏱️ 工作计时器
- 💰 存款金额
- ⌖ 定位归位按钮
- ❚❚ 暂停/播放按钮
- ↻ 重启按钮

## 新功能列表

✅ **原生窗口拖拽** - 像 Stickies 一样移动
✅ **自动散步** - 金钱豹会在桌面走动
✅ **转身动画** - 根据移动方向自动转身
✅ **固定小窗口** - 240x280px 紧凑尺寸
✅ **极简黑白UI** - 纯粹的黑白配色
✅ **定位归位** - ⌖ 按钮回到左下角
✅ **暂停/播放** - ❚❚/▶ 控制计时
✅ **重启计时** - ↻ 重新开始

## 打包结果

✅ 成功打包到: `dist/mac/money-leopard-pet.app`

## 使用方法

1. **运行开发版:**
   ```bash
   npm start
   ```

2. **打包应用:**
   ```bash
   npm run pack
   ```

3. **双击运行:** 打开 `dist/mac/money-leopard-pet.app`

## 行为特点

- 💼 工作时自动在桌面走动
- 🏃 每4秒有50%概率移动一小步
- 🔄 向左走时金钱豹会转身
- ⏸️ 暂停时停止散步
- 🎯 运动时停止散步,专心做操
- 📍 定位按钮可快速回到左下角

## 与之前版本的区别

| 特性 | 旧版 | Stickies 版 |
|------|------|------------|
| 窗口模式 | 全屏透明覆盖 | 固定小窗口 |
| 拖拽方式 | 自定义JS逻辑 | 原生窗口拖拽 |
| 散步功能 | ❌ | ✅ 自动散步 |
| UI风格 | 彩色卡通 | 极简黑白 |
| 窗口尺寸 | 覆盖所有屏幕 | 240x280px |
| 鼠标穿透 | 支持 | 不需要 |

这个版本更像一个真正的桌面宠物便签,会在你的桌面上活动!

## 文件位置

- 源码: `/Users/kaiyee/Desktop/vibe_coding/VBP012_desktop_pet/`
- 应用: `/Users/kaiyee/Desktop/vibe_coding/VBP012_desktop_pet/dist/mac/money-leopard-pet.app`
